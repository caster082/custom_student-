<odoo>
    <data>
        <!--        window action-->
        <record id="action_skill" model="ir.actions.act_window">
            <field name="name">Bio Skill</field>
            <field name="res_model">bio.skill</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
                <p>Create first Skill</p>
            </field>
        </record>

<!--         groups is module name and record id from security.xml-->
        <menuitem id="bio_skill" name="SKill" parent="education_background"
                  action="action_skill" sequence="30" groups="custom_student.group_skill"/>

    </data>
</odoo>